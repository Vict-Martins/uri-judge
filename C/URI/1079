#include <stdio.h>
#define MAX 99999

int main(){


int q,i;
scanf("%d",&q);
double a[q],b[q],c[q],m[q];
for(i = 0 ; i < q ;i++){
    scanf("%lf %lf %lf",&a[i],&b[i],&c[i]);
    m[i] = ((a[i]*2 + b[i]*3 + c[i]*5) / 10.0);
}
for(i = 0 ; i < q ;i++){
    printf("%.1lf\n",m[i]);
}

return 0;
}

