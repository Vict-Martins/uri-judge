#include <stdio.h>
#define MAX 99999
int main(){
char p[MAX] = "primeiro",s[MAX] = "segundo",t[MAX] = "terceiro",q[MAX] = "quarto";
int X[MAX],Y[MAX],i = 0,C = 0;

while(1){
    scanf("%d %d",&X[C],&Y[C]);

    if(X[C] == 0 || Y[C] == 0)
       break;

C++;
}
for(i = 0 ; i < C ; i++){
      if(X[i] > 0 && Y[i] > 0)
        printf("%s\n",p);
      else if(X[i] < 0 && Y[i] > 0)
        printf("%s\n",s);
      else if(X[i] < 0 && Y[i] < 0)
        printf("%s\n",t);
      else
        printf("%s\n",q);
}
return 0;
}
