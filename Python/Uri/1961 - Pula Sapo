import math
a, b = input('').split()
a = int(a)
b = int(b)
c = []
c = input('').split(' ')
for i in range(b): 
  if i > 0:
    sub = math.fabs(int(c[i])-int(c[i-1]))
    sub = int(sub)
    if sub > a:
      print('GAME OVER')
      break
    elif i == b-1:
      print('YOU WIN')